(function() {
    'use strict';

    angular.module('app', [
        'app.templates'
    ])
    .controller('appController', AppController);

    function AppController($rootScope, $scope) {
    }
}());
